{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "cd backend && pip install -r requirements10.txt"
  },
  "deploy": {
    "startCommand": "cd backend && python3 manage.py migrate && gunicorn backend.wsgi:application --bind 0.0.0.0:$PORT"
  }
}
